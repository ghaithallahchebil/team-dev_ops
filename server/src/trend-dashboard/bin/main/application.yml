server:
  port: 8083

spring:
  application:
    name: trend-dashboard

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always 